# SRT 仓库规范

本文件随仓库同步，协作者和智能体都要遵守。个人路径、日程和本机提示词放在 `local/`，不进入仓库。

仓库为公开仓库，位于组织 `THU-IM24` 下。提交到本仓库的内容对所有人可见，且不可收回；不要把原始数据、未定结论或本机信息写进来。

研究问题、数据定义和时间以 `README.md` 为准。不在本文件重复导师简介。

## 协作

- 仓库为公开仓库。
- `main` 只通过 Pull Request 合并。提交人不得自行合并。合并前必须由 GitHub 账号 `feiys22` 审核并批准。
- 不要直接推送 `main`，不要强推，不要改写已共享历史。
- 协作者权限用 Write。不授予 Admin 或 Owner。Owner 和 Admin 可以修改分支规则或绕过 `feiys22` 的审核，与上述合并要求冲突。
- `main` 已开启分支保护：要求 Pull Request、要求 1 个批准、禁止管理员绕过、禁止强推和删除分支。协作者若被改为 Admin 或 Owner，等于绕开这层保护，不要再给。
- 智能体不代为创建远程、邀请协作者、修改权限或合并 Pull Request，除非 `feiys22` 当次明确要求。

## 执行

- 主分析和大图计算在本机或云服务器执行，不把 GitHub Actions 当作主计算环境。
- Actions 只用于格式检查、小样本测试和可复现性核验。
- 不把密钥、原始数据和可重新生成的大结果放进 Actions 日志或仓库。

## 不要提交

- `local/` 中除 `local/README.md` 以外的文件。`.gitignore` 已排除 `local/**`。
- `.env`、密钥、令牌、本机编辑器状态。
- `raw/outputs/` 中可重新生成的产物。目录说明 `raw/outputs/README.md` 可以保留。
- 其他本机目录中的草稿。未明确要求，不从仓库外复制文件进来。

## 数据与结论

- 主数据为 Moltbook。边定义为评论者指向帖作者，去除自评。
- 写清数据覆盖、缺失与采集偏差。不把单一快照写成社区全貌。
- 集体形态和对话功能分开测量。不用发帖规模或网络外形代替交往。
- 不预设结题结论。不编造数据、文献或结果。

## 智能体

- 提问、确认、比较时先回答，不改文件。只有明确要求修改、写入、删除、执行时才动仓库。
- 不读取、不修改 `local/` 里的个人文件，除非当前使用者明确要求。
- 研究文档默认中文。
- 不把个人称呼、本机路径或其他项目的规范写进本文件。
